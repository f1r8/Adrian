micro ~/.config/micro/settings.json
```
{
    "clipboard": "terminal"
}
```
zip *

base64 InfiniteMoney.zip > base64.zip

micro base64.zip

`Ctrl-c`
`Ctrl-v`

Unlocking:

base64 -d base64.zip > thing.zip

unzip thing.zip
